Crie a página de detalhe de validação integrada ao endpoint GET /api/validacoes/:id.

# Componentes

* ValidationDetail
* BackButton

# Hooks

* useValidationDetail(id)

# Objetivo

Exibir todos os detalhes de uma validação específica.

# Requisitos

* Buscar validação por ID
* Loading state
* Error state
* Navegação de retorno
* Exibição organizada dos agentes
* Componentização limpa
* Separação de responsabilidades

# Entregue

* Código completo
* Estrutura de arquivos
* Hook reutilizável
* Exemplo de rota React Router

Antes de gerar o código:
1. Explique rapidamente a arquitetura escolhida
2. Liste a estrutura de arquivos
3. Depois gere os arquivos completos
4. Nunca omita imports
5. Nunca use pseudo-código
6. Se algum componente já existir, reutilize e refatore ao invés de duplicar lógica.